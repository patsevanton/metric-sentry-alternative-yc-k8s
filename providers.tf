# Провайдер без явного folder_id: folder задаётся на каждом ресурсе через
# local.folder_id (переменная folder_id либо client config), что исключает
# циклическую зависимость с data.yandex_client_config.
